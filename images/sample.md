
# Sample

Status: active -> (migrating to ==new system==)
Priority: high -> ==critical== (escalated)
; TODO: Client <-> Server (bidirectional sync)
// Note: Input -> Process -> (Output) -> ==Done==
__underline highlight__ and ==double-equal highlight==
Normal text (with **bold** and *italic* in brackets)

| Feature       | Status | Notes         |
|---------------|--------|---------------|
| Arrows        | done   | bidirectional |
| Tables        | done   | header + rows |
| Shell comment | done   | fenced blocks |

- Indented shell:
  ```bash
  # install and run
  npm install
  ```
